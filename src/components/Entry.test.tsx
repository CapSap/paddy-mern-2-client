import React from "react";

import Entry from "./Entry";

describe("entry comp", () => {
  it("exists", () => {
    const entry = <Entry />;
    expect(entry).toBeDefined();
  });

  it("has a form elelemt that users can input values", () => {
    const entry = <Entry />;
  });
});
